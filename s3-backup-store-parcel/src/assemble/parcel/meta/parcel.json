{
	"schema_version": 1,
	"name": "${parcel.name}",
	"version" : "${project.version}",
	"setActiveSymlink": true,
	"depends": "",
	"replaces":"${parcel.name}",
	"conflicts":"",
	"provides": ["hdfs-plugin"],
	"scripts": {
		"defines":"parcel_env.sh"
	},
	"components": [
		{
			"name" : "${parcel.name}",
			"version" : "${project.version}",
			"pkg_version": "${project.version}"
		}
	],
	"packages" : [],
	"users":  {},
	"groups": []
}
